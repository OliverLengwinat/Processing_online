void setup() {
	//printMessage(jsString + " " + processingString); //function defined in JavaScript
	if (input_image_name!=null)	printMessage(input_image_name);
	else printMessage("no Image found");
}